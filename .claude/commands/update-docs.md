Review all recent git commits since the last documentation update and update the project documentation if needed.

## Steps

1. Run `git log --oneline` to see recent commits
2. Read the current `README.md` and `CLAUDE.md`
3. Compare the documented features against what the commits describe
4. Update `README.md` if any user-facing features, controls, or game mechanics are missing or outdated
5. Update `CLAUDE.md` if any architectural details, code conventions, or technical descriptions are missing or outdated
6. Do NOT update docs if everything is already up to date — just say so
7. All user-facing text (README) must be in French

## Important

- Keep the existing structure and tone of each file
- Only add or modify sections that are genuinely outdated or incomplete
- Do not rewrite sections that are already accurate
